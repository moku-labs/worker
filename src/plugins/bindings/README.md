# bindings

> Micro plugin — stateless resolver over a request-supplied env; the binding-family dependency root.

## API

<!-- Populated during build -->

## Configuration

<!-- Populated during build -->
